{
  "name": "taskflow-pro",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend/backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend/backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node frontend/backend/server.js",
    "dev": "npx concurrently \"npm run dev --prefix frontend/backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
